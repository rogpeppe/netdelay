## netdelay: Go packages for adding delay and buffering to network connections

This provides a way of adding arbitrary network latency and buffering
to Go I/O, useful for testing network applications.
